# 乐颜 · LLM 代理配置（复制本文件为 proxy/.env，填入你的真实 key）
# key 只存在这里（服务端），浏览器拿不到。.env 已 gitignore，不会被提交。

# ===== 三选一，取消注释你要用的那家 =====

# —— DeepSeek V4 —— https://platform.deepseek.com
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-v4-pro
LLM_API_KEY=sk-在这里填入你的DeepSeek密钥

# —— 豆包 / 火山方舟 —— https://console.volcengine.com/ark
# LLM_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
# LLM_MODEL=你的接入点ID或doubao模型名
# LLM_API_KEY=在这里填入你的方舟密钥

# —— 通义千问 / 百炼 —— https://bailian.console.aliyun.com
# LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# LLM_MODEL=qwen-plus
# LLM_API_KEY=sk-在这里填入你的DashScope密钥

# 代理端口（前端默认连 8787）
PORT=8787
# 允许的前端来源（开发时可留 *，上线请改成你的域名）
CORS_ORIGIN=*
